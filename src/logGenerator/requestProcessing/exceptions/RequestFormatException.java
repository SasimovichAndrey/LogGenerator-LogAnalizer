package logGenerator.requestProcessing.exceptions;

public class RequestFormatException extends Exception{
	/*
	 * Исключение, выбрасываемое в случае недопустимого формата параметров запроса, 
	 * либо недопустимых значений параметров(например strcount < 0).
	*/
}
